name: Build and Push Docker Images

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    ### Build & Push Angular Docker Image ###
    - name: Build Angular image
      run: docker build -t oussamazaoui872/angular-app:latest ./angular-15-client

    - name: Push Angular image
      run: docker push oussamazaoui872/angular-app:latest

    ### Build & Push Spring Boot Docker Image ###
    - name: Build Spring Boot image
      run: docker build -t oussamazaoui872/springboot-app:latest ./spring-boot-server

    - name: Push Spring Boot image
      run: docker push oussamazaoui872/springboot-app:latest
